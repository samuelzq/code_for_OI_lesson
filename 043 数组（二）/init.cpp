#include <bits/stdc++.h>
using namespace std;

int main() {
    // 使用初始化列表初始化 vector
    vector<int> vec = {1, 2, 3, 4, 5};

    // 输出 vector 元素
    for (int i = 0; i < vec.size(); ++i) {
        cout << vec[i] << " ";  // 输出：1 2 3 4 5
    }

    return 0;
}

